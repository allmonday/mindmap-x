# 页内 Agent 的模型配置（部署级默认——也可在 UI 里直接配：点灰色对话按钮或
# 聊天面板齿轮图标。界面配置存 var/provider.json 且优先生效，无需重启）。
# 风格默认 OpenAI 兼容（DeepSeek/GLM/Kimi/Ollama…通用）；Anthropic 风格设 AGENT_PROVIDER=anthropic。
# 用法：cp .env.example .env 填入后 docker compose up -d；
# 不配置则页内 Agent 面板显示明确的错误横幅，画布功能不受影响。
OPENAI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
OPENAI_API_KEY=your-api-key
AGENT_MODEL=glm-5.3-flash
# AGENT_PROVIDER=anthropic

# 本地 Claude Code 对话通道（ChatPanel 第二种 Agent 形式）：全局开关，
# 默认关闭——开启后前端出现 Strands / Claude Code 形式切换条。
# LOCAL_AGENT_ENABLED=1

